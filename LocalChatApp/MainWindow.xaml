<Window x:Class="LocalChatApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Local Chat Assistant"
        Height="760"
        Width="980"
        MinHeight="640"
        MinWidth="860"
        Background="#FF0F172A">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Offline Local Chat + Image Assistant"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Foreground="#FFE2E8F0"
                   Margin="0,0,0,14" />

        <Border Grid.Row="1" Background="#FF1E293B" CornerRadius="14" BorderBrush="#FF334155" BorderThickness="1" Padding="14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Text="{Binding Prompt, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" MinHeight="110" FontSize="15" Foreground="#FFF8FAFC" Background="#FF0F172A" BorderThickness="0" Padding="10" ToolTip="Type your prompt here" />

                <Button Grid.Column="1" Margin="12,0,0,0" Width="46" Height="46" Command="{Binding DictateCommand}" FontSize="20" Background="#FF334155" Foreground="#FFF8FAFC" BorderBrush="#FF475569" Cursor="Hand" ToolTip="Speak prompt">🎤</Button>

                <Button Grid.Column="2" Margin="10,0,0,0" Width="110" Height="46" Command="{Binding AskCommand}" FontSize="16" FontWeight="SemiBold" Background="#FF0EA5E9" Foreground="White" BorderThickness="0" Cursor="Hand" Content="Ask" />
            </Grid>
        </Border>

        <TextBlock Grid.Row="2" Text="Assistant response" FontSize="16" FontWeight="SemiBold" Foreground="#FFCBD5E1" Margin="0,14,0,8" />

        <Border Grid.Row="3" Background="#FF1E293B" CornerRadius="14" BorderBrush="#FF334155" BorderThickness="1" Padding="14">
            <TextBox Text="{Binding Response}" AcceptsReturn="True" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontSize="15" Foreground="#FFF8FAFC" Background="#FF0F172A" BorderThickness="0" Padding="10" />
        </Border>

        <TextBlock Grid.Row="4" Text="Generated image" FontSize="16" FontWeight="SemiBold" Foreground="#FFCBD5E1" Margin="0,14,0,8" />

        <Border Grid.Row="5" Background="#FF1E293B" CornerRadius="14" BorderBrush="#FF334155" BorderThickness="1" Padding="14">
            <DockPanel>
                <TextBlock DockPanel.Dock="Bottom" Text="{Binding ImageStatus}" Foreground="#FF94A3B8" Margin="4,10,4,0" TextWrapping="Wrap" />
                <Image Source="{Binding GeneratedImage}" Stretch="Uniform" MaxHeight="220" />
            </DockPanel>
        </Border>
    </Grid>
</Window>
